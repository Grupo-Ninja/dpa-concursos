# ==============================================================================
# CONFIGURAÇÕES DO PROJETO DPA-CONCURSOS
# ==============================================================================
# Copie este arquivo para .env e preencha com os valores corretos.
# IMPORTANTE: O arquivo .env NÃO deve ser commitado no repositório.
# ==============================================================================

# --- BACK-END (PocketBase) ---
# Porta onde o PocketBase irá rodar
POCKETBASE_PORT=8090
# Credenciais do administrador inicial do PocketBase (usado no seed.js)
POCKETBASE_ADMIN_EMAIL=admin@email.com
POCKETBASE_ADMIN_PASSWORD=1234567890

# --- FRONT-END (Vite/React) ---
# URL da API do PocketBase. 
# Para desenvolvimento local: http://localhost:8090
# Para produção com Docker: http://backend:8090 (nome do serviço no docker-compose)
# Para produção externa: https://api.seudominio.com
VITE_API_URL=http://localhost:8090

# Porta onde o front-end irá rodar
VITE_PORT=3000

# Hosts permitidos pelo Vite (separados por vírgula)
# Exemplo: localhost,meusite.com,app.meusite.com
VITE_ALLOWED_HOSTS=localhost,dpa.concursos.plataformaninja.com

# Chave da API do Gemini (se usada pela aplicação)
GEMINI_API_KEY=

# --- CORS (PocketBase) ---
# Origem permitida para requisições CORS. Use * para desenvolvimento ou a URL do frontend em produção.
# Exemplo: http://localhost:3000 ou https://app.seudominio.com
POCKETBASE_CORS_ORIGIN=*
